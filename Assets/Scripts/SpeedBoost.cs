using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//public class SpeedBoost : MonoBehaviour
//{
   // [SerializeField] private float boostMultiplier = 10f; // Mno¿nik przyspieszenia
   // [SerializeField] private float boostDuration = 2f;   // Czas trwania przyspieszenia
    //[SerializeField] private ParticleSystem Particles;

   //private void OnTriggerEnter(Collider other)
   // {
        //// Sprawdzamy, czy obiekt, który wszed³ w trigger, to gracz
       // Gracz gracz = other.GetComponent<Gracz>();
       // if (gracz != null)
       // {
       //     gracz.ApplySpeedBoost(boostMultiplier, boostDuration);
       // }
   // }
   
//}
